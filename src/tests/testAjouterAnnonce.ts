import {NavigateurBacASable, ApplicationClient} from '../coeur/bacasable';
import * as Pages from '../client/allPages';

export class TestAjouterAnnonce
{
    test(nav:NavigateurBacASable, applicationClient:ApplicationClient)
    {
        /*
        var pageAccueil = nav.Vers(Rediriger.Vers(Pages.PageAccueil, null));
        var pageConnection = nav.Vers(pageAccueil.seConnecter);

        pageConnection.utilisateur = 'etienne';
        pageConnection.motDePasse = 'biiiiip';
        
        var redirection = <Redirection<Pages.PageAccueilConnecté>> pageConnection.seConnecter();
        var pageAjouterAnnonce = nav.Vers(redirection);

        return nav;
        */
    }
}
